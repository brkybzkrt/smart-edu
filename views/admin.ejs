<%- include('partials/_header.ejs') %>

    
	<%- include('partials/_nav') %>
	


<% if(user.role==='Admin') {%>
    <div class="all-title-box">
        <div class="container text-center">
            <h1><%=user.name%><span class="m_1">ADMİN</span></h1>
        </div>
    </div>


    <div id="overviews" class="section wb">
        <div class="container">
            <div class="row"> 
                <div class="col-lg-12 blog-post-single">					
                    <div class="blog-author">
                        <div class="author-bio">
                            <h3 class="">Users</h3>
                            
                        
                        </div>
                        
                    </div>

                </div><!-- end col -->
            </div><!-- end row -->
            <div class="row mt-5"> 
                <table class="table">
                    <thead>
                      <tr>
                        <th scope="col">#</th>
                        <th scope="col">Name</th>
                        <th scope="col">Email</th>
                        <th scope="col">Role</th>
                        <th scope="col">Delete</th>
                      </tr>
                    </thead>
                    <tbody>
                        <% for(let i =0;i<users.length;i++) {%>
                      <tr>
                        <th scope="row"><%=i+1%></th>
                        <td><%= users[i].name%></td>
                        <td><%= users[i].email%></td>
                        <td><%= users[i].role%></td>
                        <td> <a href="/admin/delete/<%= users[i]._id%>?_method=DELETE" class="btn btn-danger <%=users[i].role==='Admin' && 'disabled' %>" onclick="return confirm('are you sure')" >Delete</a></td>
                      </tr>
                     <%}%>
                    </tbody>
                  </table>
            </div><!-- end row -->
        </div><!-- end container -->
    </div><!-- end section -->

<%   } %>

<%- include('partials/_footer') %>
